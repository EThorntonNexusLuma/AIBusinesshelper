# Environment Variables for LumX API Server
# Copy this file to .env and fill in your actual values

# OpenAI API Key (required for AI chat responses)
OPENAI_API_KEY=your-openai-api-key-here

# Server Configuration
PORT=5174

# Optional: Zapier webhook URL for lead forwarding
# ZAPIER_HOOK_URL=https://hooks.zapier.com/hooks/catch/your_webhook_id/

# Optional: Database connection (if using Supabase, MongoDB, etc.)
# DATABASE_URL=your_database_connection_string

# Optional: Email service configuration (if using nodemailer, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password